# Example environment configuration (not used directly by docker-compose)
# Values here are for reference only if you run the app outside Docker.

MONGO_HOST=localhost
MONGO_PORT=27017
MONGO_DB=utkrusht_multitenant
MONGO_USER=utkrusht_user
MONGO_PASSWORD=utkrusht_pass
APP_PORT=3000
